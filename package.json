{
    "name": "flappybird",
    "version": "1.0.0",
    "description": "Flappy Bird game using Phaser.js",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "dev": "nodemon server.js",
        "build": "mkdir -p dist && cp -r index.html game.js sprites audio dist/"
    },
    "dependencies": {
        "express": "^4.18.2",
        "phaser": "^3.60.0"
    },
    "devDependencies": {
        "nodemon": "^2.0.22"
    },
    "engines": {
        "node": ">=14.0.0"
    }
}